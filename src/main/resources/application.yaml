spring:
  application:
    name: parkizo

  datasource:
    url: jdbc:postgresql://localhost:5432/parkizo
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

  flyway:
    enabled: true